# FastAPI DonFitness

Управляйте фитнес-клубом, отслеживайте данные о тренерах, резидентах и записях на тренировки с помощью мощного FastAPI бэкенда.

## Оглавление
- [Описание проекта](#project-info)
- [Стек технологий](#tech-stack)
- [Инструкция по запуску](#project-running-docker)
- [Запуск без Docker (для локальной разработки)](#project-running-ide)
- [Запуск тестов](#project-tests)
- [Как использовать](#how-to-use)
- [Использование Postman](#use-postman)
- [Развертывание (Deployment)](#deployment)
- [Лицензия](#license)
- [Автор](#author)


## Описание проекта <a id='project-info'></a>

Проект FastAPI предназначен для управления фитнес-клубом.

Приложение предоставляет такие возможности, как:

- регистрация/авторизация пользователя;
- создание/редактирование тренировок, видов тренировок и тренеров;
- учёт резидентов фитнес-клуба, их абонементов, достижений и записей на тренировки;
- поиск нужной тренировки по фильтру (дата, вид тренировки, тренер и т.д.);
- запись/отмена записи на тренировку;
- просмотр истории тренировок.


## Стек технологий <a id='tech-stack'></a>

- ⚡ [**FastAPI**](https://fastapi.tiangolo.com) для Python backend API.
  - 🧰 [SQLModel](https://sqlmodel.tiangolo.com) для взаимодействия Python с базой данных (ORM).
  - 🔍 [Pydantic](https://docs.pydantic.dev), используется FastAPI для проверки данных и управления настройками.
  - 💾 [PostgreSQL](https://www.postgresql.org) основная СУБД.
- 🐋 [Docker Compose](https://www.docker.com) для разработки и продакшена.
- 🔒 Безопасное хеширование паролей по умолчанию.
- 🔑 JWT (JSON Web Token) аутентификация.
- ✅ Тесты на [Pytest](https://pytest.org).
- 🚢 Инструкции по развертыванию с использованием Docker Compose, в том числе о том, как настроить внешний прокси-сервер Traefik для обработки автоматических сертификатов HTTPS.
- 🏭 CI/CD на основе GitHub Actions.





## Инструкция по запуску <a id='project-running-docker'></a>

### Предварительные требования
Для успешного запуска проекта на вашем ПК должны быть установлены:
- [**Git**](https://git-scm.com/downloads)
- [**Docker Desktop**](https://www.docker.com/get-started/) (или Docker Engine и Docker Compose)


### 1. Установка Docker на ПК

Платформа Docker позволяет запускать программное обеспечение без установки необходмиых средств разработки, языков программирования, IDE и нужных библиотек.

Для начала работы с [**Docker**](https://www.docker.com/get-started/), перейдите по ссылке, сделав двойной клик, и установите Docker на свой ПК. Затем запустите программу и не закрывайте во время запуска проекта.

### 2. Клонирование репозитория

- Откройте терминал и перейдите на рабочий стол или в иную директорю, в которой вы хотите разместить проект, например:

```bash
cd ~/Desktop
```

- Склонируйте репозиторий проекта `FastAPI_DonFitness` на рабочую директорию:

```bash
git clone https://github.com/nikitakisel/donfitness_api
```

- Перейдите в папку проекта:

```bash
cd donfitness_api
```

### 3. Запуск проекта в Docker

- Убедитесь, что вы находитесь в корне проекта (donfitness_api/):

```bash
pwd
```

- Соберите образ контейнера и разверните его с помощью docker-compose:

```bash
docker-compose up --build
```

- Если вы всё сделали правильно, то после некоторого времени ожидания в терминале должно появиться следующее сообщение:

```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

Поздравляю, вы успешно запустили проект!


### 4. Остановка проекта

Чтобы остановить все сервисы и удалить контейнеры:

```bash
docker-compose down
```

Для удаления образов и томов (при этом будут удалены данные базы данных):

```bash
docker-compose down -v --rmi all
```

Опционально: для очистики мусора, созданного Docker, можно воспользоваться следующей командой:

```bash
docker system prune -a
```


## Запуск без Docker (для локальной разработки) <a id='project-running-ide'></a>

Если вы хотите запустить проект напрямую (например, для отладки в IDE):

- Создайте и активируйте виртуальное окружение:
  
```bash
python -m venv .venv
source .venv/bin/activate # Для Linux/macOS
# .venv\Scripts\activate # Для Windows
```

- Установите зависимости:
  
```bash
pip install -r requirements.txt
```

- Настройте переменные окружения: Убедитесь, что ваш .env файл корректно настроен и переменные доступны для приложения (например, с помощью python-dotenv). Пример оформления файла .env:

```dotenv
# Database
DATABASE_URL="postgresql://postgres:postpass@localhost:5432/donfitness"

# Security
SECRET_KEY="your_secret_key"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=120

# CORS Origins
CORS_ORIGINS='["http://localhost:3000", "http://localhost:8000"]'
```

- Запустите сервер Uvicorn:
  
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```


## Запуск тестов <a id='project-tests'></a>

Для запуска тестов используйте следующую команду:

```bash
docker-compose run --rm backend pytest
```


## Как использовать <a id='how-to-use'></a>

После успешной сборки и развёртывания проекта [документация FastAPI](http://127.0.0.1:8000/docs#/) доступна по клику по ссылке.

В FastAPI используется удобная документация в виде SwaggerUI, которая должна выглядеть следующим образом:

![swagger-ui-1.png](img%2Fswagger-ui-1.png)

В ней доступна информация о всех реализованных в приложении ендпоинтах:

![swagger-ui-2.png](img%2Fswagger-ui-2.png)

При нажатии на любой из них, появляется информация о запросе, о том, какие параметры и какое тело необходимо в него передать, чтобы он успешно выполнился:

![swagger-ui-3.png](img%2Fswagger-ui-3.png)


## Использование Postman <a id='use-postman'></a>

Чтобы протестировать API вручную и "пощупать" его функционал, можно установить [Postman](https://www.postman.com/downloads/), который позволяет удобно выполнять HTTP-запросы и сохранять историю их выполнения.

Postman Collection:
[Ссылка на Postman Collection](link_to_your_postman_collection.json) (Экспортируйте свою коллекцию и разместите ее где-нибудь, например, на GitHub Gist, или добавьте ее в репозиторий).

## Развертывание (Deployment) <a id='deployment'></a>

Проект разработан с учетом легкости развертывания с использованием Docker Compose. Для продакшена рекомендуется использовать внешний прокси-сервер, такой как Traefik, для управления SSL-сертификатами (Let's Encrypt) и маршрутизацией запросов. Подробные инструкции по настройке Traefik и развертыванию доступны [здесь](link_to_your_deployment_docs.md).

## Лицензия <a id='license'></a>

Этот проект распространяется под лицензией [MIT](LICENSE).

## Автор <a id='author'></a>

Никита Киселев - [GitHub](https://github.com/nikitakisel/)